import { readFile } from "node:fs/promises";
interface PreservationInput{lockedDecisions?:unknown;openDecisions?:unknown;requestedChanges?:unknown;reopenedDecisions?:unknown}
const input=process.argv[2];if(!input){console.error("Usage: validate-preservation.ts <artifact.json>");process.exit(2)}let value:PreservationInput;try{value=JSON.parse(await readFile(input,"utf8")) as PreservationInput}catch(error){console.error(`Invalid preservation JSON: ${error instanceof Error?error.message:String(error)}`);process.exit(1)}const strings=(v:unknown):string[]=>Array.isArray(v)?v.filter((x):x is string=>typeof x==="string"):[];const locked=strings(value.lockedDecisions),open=strings(value.openDecisions),changes=strings(value.requestedChanges),reopened=strings(value.reopenedDecisions);const contradictions=locked.filter(d=>open.includes(d));if(contradictions.length){console.error(`Locked/open contradiction: ${contradictions.join(", ")}`);process.exit(1)}const illegal=changes.filter(d=>locked.includes(d)&&!reopened.includes(d));if(illegal.length){console.error(`Requested change touches locked decision without reopening: ${illegal.join(", ")}`);process.exit(1)}console.log(JSON.stringify({valid:true,locked,open,requestedChanges:changes,reopened},null,2));
